{
  "name": "aws-node-sls-starter",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "serverless offline",
    "deploy": "serverless deploy --stage dev --region us-east-1",
    "deploy:prod": "serverless deploy --stage prod --region us-east-1",
    "remove": "serverless remove",
    "info": "serverless info --verbose",
    "logs:worker": "serverless logs -f queueWorker -t"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-offline": "^13.6.0",
    "serverless-offline-sqs": "^8.3.0"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.600.0"
  }
}