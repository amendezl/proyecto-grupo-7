# Relativized values - set these via environment variables or deployment scripts
NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-https://your-api-id.execute-api.${AWS_REGION:-us-east-1}.amazonaws.com}
NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL:-wss://your-ws-id.execute-api.${AWS_REGION:-us-east-1}.amazonaws.com/${STAGE:-dev}/}
NEXT_PUBLIC_COGNITO_USER_POOL_ID=${NEXT_PUBLIC_COGNITO_USER_POOL_ID}
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=${NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID}
NEXT_PUBLIC_STAGE=${STAGE:-prod}
NEXT_PUBLIC_AWS_REGION=${AWS_REGION:-us-east-1}
NEXT_PUBLIC_S3_BUCKET=${S3_BUCKET_NAME:-sistema-gestion-espacios-frontend-${STAGE:-prod}}